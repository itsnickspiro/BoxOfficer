{
  "name": "boxofficer-functions",
  "version": "1.0.0",
  "description": "BoxOfficer Firebase Cloud Functions — secure API proxy",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed'",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "node-fetch": "^2.7.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "20"
  }
}
